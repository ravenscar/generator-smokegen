language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-smokegen' ]; then cd .. && eval "mv $currentfolder generator-smokegen" && cd generator-smokegen; fi
